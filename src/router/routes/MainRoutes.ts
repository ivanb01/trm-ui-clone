export enum MainRoutes {
  ERROR = 'error',
  HOME = '/',
  INSIGHTS = 'insights',
  LOGIN = 'login',
  LOSES = 'loses',
  MAP = 'map',
  READINGS = 'consumption',
  SETTINGS = 'settings',
  TRANSFORMER = 'transformer-station'
}
